{
  "name": "subgraph-to-learn",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph-to-learn",
    "create-local": "graph create --node http://localhost:8020/ subgraph-to-learn",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph-to-learn",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph-to-learn"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
